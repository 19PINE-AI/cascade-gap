__pycache__/
*.pyc
.venv/
venv/
env/

# Data, model weights, and runs (large; not for git).
# /models/ is anchored to repo root so harness/models/ (python code) stays tracked.
/data/
/runs/
/cache/
/models/
*.wav
*.mp3
*.mp4
*.png
*.jpg
*.pdf
